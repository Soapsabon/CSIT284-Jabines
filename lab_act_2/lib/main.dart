import 'package:flutter/material.dart';
import 'dice_roller.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        body: DiceRoller(),
      ), // Scaffold
    ), // MaterialApp
  );
}