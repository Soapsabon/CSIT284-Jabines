import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.indigo,
        body: Container(
          child: Center(
            child: Text("Hello World"),
          ), // Center
        ), // Container
      ), // Scaffold
    ), // MaterialApp
  );
}